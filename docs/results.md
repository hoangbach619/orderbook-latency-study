# Results

Placeholder. This document is populated in a later commit, once the depth sweep across the
variants has been run on a documented machine.

It will hold the measured latency distributions for each variant and operation, the
hardware counter figures that explain them, the location of the crossover where the linear
scan stops beating the tree, and a link to the machine record in `docs/hardware.md` for the
run. Raw results will also be exported as CSV.

No numbers are recorded here yet, because no measurement has been taken on a controlled
machine yet. Quoting figures before then would contradict the methodology this study is
built around.
